# Let the user enter in 2 numbers and giving all the odd numbers between them.

def odd(lower_limit, upper_limit):
    lower_limit = int(input('Pick a starting point: '))
    upper_limit = int(input('pick a stopping point: '))
     
    
def main():
    while (True):
        range(lower_limit, upper_limit)
            if(num%2 != 0):
                print (num)
main()